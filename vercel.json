{
  "functions": {
    "app/api/merge/route.ts": {
      "maxDuration": 30,
      "memory": 1024
    }
  },
  "installCommand": "apt-get update && apt-get install -y ghostscript && npm install",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "buildCommand": "npm run build"
}